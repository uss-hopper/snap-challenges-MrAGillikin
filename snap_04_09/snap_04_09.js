/*
  addInt is a function to add two numbers.
 */
function addInt (num1, num2) {
	//console.log("Adding " + num1 + " and " + num2 + "gives: ");
	//console.log(num1 + num2);
	return num1 + num2;
}

/*
  concatenate is a function to concatinate two strings
 */
function concatenate (phrase1, phrase2) {
	combination = phrase1.toString() + phrase2.toString();
	//console.log(combination);
	return combination;
}

addInt(6,7);
concatenate("Locked", " on");

